export interface ProfessioanlResponse {
  id?: string;
  name: string;
  phone: string;
  email: string;
  role: string;
}

export interface ProfessioanlRequest {
  name: string;
  email: string;
  phone: string;
}